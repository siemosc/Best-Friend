"""Контракт границы stt: порт транскрипции аудио в текст."""

from typing import Protocol


class SpeechTranscriber(Protocol):
    """Порт распознавания речи: байты аудио → текст."""

    async def transcribe(self, audio: bytes, filename: str) -> str | None:
        """Транскрибирует аудио.

        Возвращает строку — транскрипт (пустая строка означает, что речи в записи
        нет); None — отказ источника (сеть, ошибка сервера, нечитаемый ответ).
        Имя файла нужно источнику для определения контейнера (ogg, mp3).
        """
        ...
