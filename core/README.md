# core

BestFiend как модульный монолит. Единый процесс, держит control_plane,
graph, telegram, memory и artifacts capability.

Запуск: `uv run python -m bestfiend` (порт — env `CORE_PORT`, дефолт `8010`).
